<article id="{{slug}}" class="home-post">
	<a href="{{url absolute=true}}"><h2 class="post-title">{{title}}</h2></a>
	<p class="post-meta text-muted">
		<small>
			{{#primary_author}}<a href="{{url absolute='true'}}">{{name}}</a>{{/primary_author}} | {{date published_at format="DD MMM YYYY"}}
		</small>
	</p>
	{{#if feature_image}}
	<a class="post-image shadow lazy" data-src="{{img_url feature_image absolute='true'}}"></a>
	{{/if}}
	<p class="post-excerpt text-muted">{{excerpt characters="306"}}</p>
	<a class="post-readmore" href="{{url absolute='true'}}">Leggi di più</a>
	<hr class="post-sep">
</article>
